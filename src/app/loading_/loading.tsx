// Esse file deveria ficar na pasta App. Colocamos aqui para não dar conflito do component loading do react
/* import { SpinLoader } from '@/components/SpinLoader';

export default async function LoadingRoot() {
	return (
		<div>
			<SpinLoader className="min-h-screen" />
		</div>
	);
}
 */

// NOTE => VAMOS UTILIZAR O <suspense> DO REACT
